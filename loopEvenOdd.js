// Write a for loop iterating from 1 to 20 and logs whether each number is "even" or "odd".

for (let i = 1; i < 20; i++) {
  if (i % 2 === 0) {
    console.log(`"${i} is even"`);
  } else {
    console.log(`"${i} is odd"`);
  }
}
